# eslint-plugin-jsx-a11y v6.10.2 declares peer eslint ^3..^9 but is
# runtime-compatible with eslint v10 (no v10-specific API breakage
# observed). Until upstream bumps the peer range, install resolves with
# legacy peer semantics in both local + CI.
legacy-peer-deps=true
